create table Todo(
    id bigint,
    username varchar(200),
    description varchar(2000),
    targetDate Date,
    isDone boolean,
    primary key(id)
);

-- create table users(
--     userid bigint,
--     username varchar(200)
--     pass_word varchar(200)
--     primary key(userid)

-- );